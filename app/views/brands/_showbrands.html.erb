
<table id="brands" class="display">
  <thead>
    <tr>
      <th>Brand Logo</th>
      <th>Brand Name</th>
      <th>Actions</th>
  

    </tr>
  </thead>
  <tbody>
   
 <% @brands.each do |b| %>

      <tr>
        <td></td>
        <td><%= b.name %></td>
      
        <td>

       
    <%= render 'edit' %>
  <button class="btn btn-primary" data-toggle="modal" href="#EditInventory"+("12") >Edit</button>
 
          <%= link_to "Edit" ,edit_inventory_path(inventory.id) , :class => 'btn btn-small btn-info' %> 
          <%= link_to "Delete" , inventory , :class => 'btn btn-small btn-danger', 
          :confirm => 'Are you sure you want to delete this?', :method => :delete %>

          <%= link_to 'Destroy', inventory, confirm: 'Are you sure?', method: :delete, :remote => true %> 


       </td>
      </tr>

    <% end %> 

  </tbody>
</table>

